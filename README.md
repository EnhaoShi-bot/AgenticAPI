# AgenticAPI

## 背景

Agentic API 是一个基于Agent的API中转站平台，可支持通过Agent来管理不同的API接口。

其采用Vue3 + TypeScript 前端框架，后端使用fastAPI + python进行开发，并为后续Agent原生功能提供支持。

其基础功能点为向外提供大模型API接口的能力，后续可拓展支持MCP、Skills、CLI等连接管理功能。


## 构建流程
### 开发模式

```sh
npm run dev
```

### 生产模式

```sh
npm run build
```
