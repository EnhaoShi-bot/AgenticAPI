# 开发模式配置（复制为 .env.development，npm run dev 时由 Vite 加载）
# /api/* 请求的代理目标：本机后端地址，端口与 backend/.env 的 SERVER_PORT 保持一致
VITE_BACKEND_URL=http://localhost:2027

# 站内接口基地址（默认 /api 走上面的代理；前后端分域部署时可改为后端完整地址，一般不动）
# VITE_API_BASE=/api
